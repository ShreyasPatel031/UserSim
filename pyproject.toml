[project]
name = "usersim"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.28.0",
    "pydantic>=2.0",
    "typing-extensions==4.15.0",
    "browser-use==0.13.8",
    "browserbase>=1.0.0",
    "playwright>=1.62.0",
]

[tool.vercel]
entrypoint = "mvp.server:app"
